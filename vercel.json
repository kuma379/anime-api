{
  "version": 2,
  "buildCommand": "cd artifacts/anime-web && npm install --legacy-peer-deps && ./node_modules/.bin/vite build --config vite.config.vercel.ts",
  "outputDirectory": "artifacts/anime-web/dist/public",
  "functions": {
    "api/index.ts": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index.ts" },
    { "source": "/:path*", "destination": "/index.html" }
  ]
}
